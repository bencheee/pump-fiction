# Project state

## Current phase

The functional specification, textual mobile-wireframe decisions, local-MVP acceptance criteria, development governance, repository-native project-management system, local technical architecture, MVP Feature breakdown, and external UI/UX collaboration process are accepted. `M-001`, `M-002`, and `M-003` are `Done`, and on `2026-09-07` the application was deployed to Vercel and hosted Supabase, which closes the production-protection open question through [ADR-0031](decisions/0031-shared-password-protects-the-hosted-application.md). `M-004` is active through [`F-017`](project/features/F-017-compact-workout-chrome-and-faster-transitions.md) and [`T-055`](project/tasks/T-055-compact-and-accelerate-workout-flow.md): compact the workout chrome and remove diagnosed redundant reads from Today, Start Workout, and Review & Finish.

## Conceptually completed

- Product boundary and four-destination mobile information architecture
- Exercise types, load modes, band semantics, notes, and archiving
- Programs, splits, ordering, activation, and normal rotation rules
- Active-workout snapshot, set entry, timer, persistence, and finish lifecycle
- Workout, exercise, and split History behavior, PR rules, and derived statistics
- Weight and user-defined body-measurement behavior
- Logical domain entities and template/snapshot separation
- Local-first delivery direction, with the Vercel/Supabase target reached on `2026-09-07`
- Testable local-MVP acceptance criteria with stable IDs (locked)
- Documentation-as-system-of-record and approval-gated feature-testing rules
- Complete repository-native project-management workflow, templates, dashboard, and registry
- Ordered Local MVP Feature breakdown, external-design workflow, and context-efficient documentation rules
- Frozen external mobile design source, tokens, specifications, assets, and structural reference scope

The [`INDEX.md`](INDEX.md) routes to each canonical specification.

## Accepted and locked decisions

- Private, single-user, phone-only product with no local-phase accounts or login: [ADR-0001](decisions/0001-private-mobile-only-app.md)
- Workout snapshots are separate from mutable templates while retaining source references: [ADR-0002](decisions/0002-template-snapshot-history-model.md)
- All historical records and statistics are grouped under History: [ADR-0003](decisions/0003-history-information-architecture.md)
- Develop and run locally before a future Vercel/Supabase production phase: [ADR-0004](decisions/0004-local-first-development.md)
- Every architectural decision and affected canonical document stays recorded and current: [ADR-0005](decisions/0005-documentation-as-system-of-record.md)
- Feature testing waits for explicit confirmation of the relevant commit: [ADR-0006](decisions/0006-approval-gated-feature-testing.md)
- Version-controlled repository Markdown is the canonical project-management source of truth: [ADR-0007](decisions/0007-repository-native-project-management.md)
- Managed work uses a Milestone → Feature → Task hierarchy, with Tasks targeted to one delivery commit: [ADR-0008](decisions/0008-milestone-feature-task-hierarchy.md)
- The user is Owner and sole Approver; every Task names an Executor and Reviewer: [ADR-0009](decisions/0009-project-management-roles.md)
- Tasks separate Backlog, execution, review, user approval, authorized testing, and completion: [ADR-0010](decisions/0010-task-lifecycle-and-test-gate.md)
- Work items use immutable `M-###`, `F-###`, and `T-###` identifiers: [ADR-0011](decisions/0011-stable-work-item-identifiers.md)
- Planning uses ordered `Now`, `Next`, and `Later` horizons, one in-progress Task per Executor, and actual event timestamps: [ADR-0012](decisions/0012-now-next-later-planning.md)
- Stable work-item files feed a concise root dashboard and complete registry without moving completed records: [ADR-0013](decisions/0013-project-artifact-layout.md)
- One immutable Task delivery commit is reviewed, approved, and tested; narrow evidence commits record the later events without changing scope: [ADR-0021](decisions/0021-delivery-and-evidence-commit-model.md)
- Explicit Definition of Ready and Definition of Done gates apply to Tasks, Features, and Milestones: [ADR-0015](decisions/0015-readiness-and-completion-gates.md)
- Dashboard and registry reporting keep current work, ownership, timing, approvals, and next action visible: [ADR-0016](decisions/0016-operational-reporting-and-projections.md)
- Runtime uses Next.js 16 Active LTS with App Router, React 19, strict TypeScript, and Node.js 24 LTS: [ADR-0017](decisions/0017-nextjs-app-router-runtime.md)
- Local persistence uses Supabase PostgreSQL, declarative SQL schemas, reviewed versioned migrations, generated TypeScript database types, no initial ORM, and a server-only repository boundary: [ADR-0018](decisions/0018-local-supabase-postgres-and-server-data-access.md)
- The repository contains one npm-managed Next.js app with explicit App Router, feature, server, and shared boundaries; active-workout changes use idempotent revisioned commands and a narrow IndexedDB pending outbox: [ADR-0019](decisions/0019-application-boundaries-and-active-workout-durability.md)
- Mobile UI uses Tailwind CSS 4, application-owned tokens/primitives, and selective Radix; charts use Recharts 3 behind a neutral data boundary; static checks and future approval-gated test tools are explicit: [ADR-0020](decisions/0020-mobile-ui-charting-and-quality-tooling.md)
- External UI/UX work uses an Owner-approved design brief and a separate versioned handoff; fixed visual references define objective fidelity: [ADR-0022](decisions/0022-versioned-external-design-handoff.md)
- The audited v0.4 prototype/specifications override the retained v0.3 PNGs only for documented color/contrast tokens, save/validation/outcome cue placement, and corrected S09/S10 validation fixtures: [`design/T-004-v0.4-frozen/README.md`](design/T-004-v0.4-frozen/README.md)
- One shared password gates the hosted application, and the hosted database stays closed by grant rather than by Row Level Security: [ADR-0031](decisions/0031-shared-password-protects-the-hosted-application.md)
- Nutrition and calorie tracking are outside product scope, not open questions.
- Local-MVP behavior and release boundary: [`product/mvp-acceptance-criteria.md`](product/mvp-acceptance-criteria.md)

Accepted core product behavior is canonical in the topic documents, not duplicated here.

## Accepted post-MVP or pre-production work

Export/backup, PWA installation, and explicit protection against accidental active-workout closure are accepted post-MVP capabilities. Production-access protection was required before deployment and is now decided by [ADR-0031](decisions/0031-shared-password-protects-the-hosted-application.md). Active-workout auto-save and restore remain part of the locked local MVP.

## Open questions

These are not decisions and must not be inferred during implementation:

- PWA implementation details;
- backup/export format and priority;
- whether and how to support estimated 1RM;
- whether and how to support RIR/RPE;
- whether and how to support a rest timer;
- whether and how to model warm-up sets;
- application name;

## Next planned step

`T-055` is `In Progress`. Its trace found application-owned latency beyond any hosted cold start: Today performs per-split reads after its aggregate, start hydrates the workout in a second request and Current Workout eagerly loads the entire exercise library, and Review & Finish refetches the workout already held by the client. The Task removes those round trips together with the Owner's compact header, disclosure, and finish-trigger request; no feature test runs before approval.

`T-027` delivery `9b8247f73bf9347cdd44f23e5172c16b9b99cfae` is approved and verified, so a clean reset now lands on the committed seed baseline and `npm run db:snapshot` and `npm run db:restore` carry the Owner's own data across a verification. `F-013` is `Done` with the Owner's confirmed result on `2026-09-05`: a reset lands on the committed seed baseline, `npm run db:snapshot` and `npm run db:restore` carry the Owner's own data across a verification, the repository suite no longer leaves an exercise behind, and its need for a database without a resumable workout is recorded next to the verification gate. `F-014` is now the current focus: `T-028` reduces the exercise types to `weights` and `bodyweight` under [ADR-0026](decisions/0026-two-exercise-types-with-assistance-under-bodyweight.md). Its first delivery failed on exercise-form component tests it had not updated; approved replacement `14fdd0ac8785127e2407584afd9a201aeaeb2cb2` then passed complete verification, so `T-028` is `Done`. `T-029` removes explicit set confirmation under [ADR-0027](decisions/0027-a-set-is-recorded-by-its-values.md): `workout_sets.is_confirmed` is dropped and whether a set counts is derived from its values. `F-014` is `Done` with the Owner's confirmed result on `2026-09-05`. `F-012` is now the current focus: `T-026` delivery `90875783eda308cdb95b33ad43a336bbd6060ccd` is approved and verified, so a refused command is now terminal, the client refreshes and replays the rest without waiting for a gesture, and the screen names the lost change. `F-012` is `Done` with the Owner's confirmed result on `2026-09-05`, and `M-001` stands at 11/14 required Features Done. The Owner directed on the same day that `F-008` History and Statistics is not to be started; it also still has no Task breakdown, and its scope boundary against `F-009` is part of that planning. There is no active work item. `F-008` follows. On `2026-09-05` the Owner asked for the `F-008` Task breakdown without starting implementation: `T-031` through `T-036` are recorded in `Backlog`, operations before screens, and the Feature file carries the proposed History-shell boundary against `F-009` and six readiness questions, chiefly whether a never-nulled identity snapshot should preserve exercise and split identity after deletion. On the same day the Owner released the hold, accepted every recommended answer and local decision, and gave the go-ahead for the whole Feature, so `T-031` was delivered and approved as `95de9212848755c956cb0dc5d50b5cfc8796dc27` on `2026-09-05`. Its authorized verification then failed on two prepared pgTAP suites: the schema change was not carried into the direct `workout_exercises` inserts of the core-constraints suite, and the new History suite started a split as an alternate after rotation had already made it the proposed one. Both were test-source defects, so replacement `b5e4cda609d478453eccd562087d1f18bfec7f54` corrects the two suites and one inaccurate sentence about what a correction returns. The Owner approved that replacement on `2026-09-05`, and its verification then passed pgTAP 112/112, unit 86/86, and the generated-type comparison before the repository script failed: it runs its files in parallel against one shared database, and this Task added a third file that starts a workout. Second replacement `c00c6e92072c98aec5e0e449797bb154bf314ccf` serializes that script with `--no-file-parallelism` and records why serial execution is a correctness requirement; the Owner approved it on `2026-09-05` and the complete plan then passed: pgTAP 112/112, repository 5/5, unit 86/86, matching generated types, and a faithful restore. `T-031` is `Done`, so History now has its reads and its ten transactional corrections, and `T-032` delivered the History shell and the Workouts subsection on them as `578cd501bf6c2ee405ca33fd9678eb9835a1fd94`. The Owner approved it and its verification passed unit and component 98/98 before the browser scenario found a real defect: an empty historical set offered no load field, because the correction form read the set's mode column instead of deriving the mode from the exercise definition as ADR-0023 requires. Replacement `c2b6fa124ed3cb327f756fc7a6fd74bd3080bdcf` derives the mode from the definition, offers the optional addition per set, and the Owner approved it on `2026-09-05`, and its verification then passed unit and component 99/99 and cleared the correction in the browser, confirming the fix, before an ambiguous Today assertion in the test itself stopped the scenario. Second replacement `35790c78201f76c0c2cec3c76bddaa8415c9727a` scopes that assertion and the four others whose names repeat on the page, and the Owner approved it on `2026-09-05`, and the complete plan then passed: unit and component 99/99 and the serialized Chromium and WebKit History scenario 2/2 with four structural captures. `T-032` is `Done`, so History has its destination shell, its month-grouped list, its saved-workout detail, and its correction screen. `T-033` delivered the exercise statistics behind `S15` and `S16` as `bf787976b69602ed4358eb79f053d48486c3d30e`, which the Owner approved and whose verification then passed pgTAP 124/124, repository 6/6, and the type comparison before two arithmetic expectations in its own unit suite failed, which approved replacement `d1f15d90151d3a7f43786da52dd3c8affc633a11` corrected. The complete plan then passed: unit 118/118, pgTAP 124/124, repository 6/6, matching types, and a faithful restore. `T-033` is `Done` and `T-034` delivered `S15` and `S16` on it as `b5772adb87d244bfc2404481e90f58a4046a7767`, including the first chart in the application. The Owner approved it and accepted that it had also swept in a concurrent `F-009` Task file; its verification then failed two ambiguous list queries, which approved replacement `b50df7b355bdaf6ab47ad1d763ce537cb0bcbd73` corrected by naming both value lists. Its own verification then found a weights exercise opening its chart on reps, so second replacement `4d4f895fc5807cffb172ec5d2a343d68bca31b8e` gave the metric list a deliberate order. That day the Owner directed that replacements stop requiring re-approval, recorded as [ADR-0028](decisions/0028-replacements-inherit-task-approval.md); a third, test-only replacement `aed262314e9c332198067bfbdd1211c221ece256` then passed the complete plan under the inherited approval, and `T-034` is `Done`. `T-035` delivered the split statistics behind `S17` and `S18` as `92b6d10b3472a13282c41712e3e75f939216f647`, which the Owner approved and which passed its complete plan on the first run: unit 137/137, pgTAP 135/135, repository 7/7, matching types, and a faithful restore. `T-035` is `Done` and `T-036` delivered `S17` and `S18`, the last screens of `F-008`, as `e5f9bf82970ca37c213fb84fef554a4d37593a78`, which the Owner approved; one test-only replacement then passed the complete plan under the inherited approval, unit 143/143 and Chromium and WebKit 2/2, so `T-036` is `Done` and every History screen is delivered. `T-037`, the last `F-008` Task, delivered the repair of the stale browser specs as `d65b0b092e04ab17761c11a4c78dd6648369a8a7`, which the Owner approved; its first run passed 25 of 32 and found two test-infrastructure faults: the durability spec has been unrunnable since `T-009`, because its harness is production-hidden while the suite runs a production server, and WebKit typed the first entry after a navigation before React had hydrated the field. Replacement `060bf92e4f28ef44006e39c75561cbae973ae724` starts a development server beside the production one for that spec alone, waits for hydration before the first entry, inherits the approval under ADR-0028, and the complete suite then passed 32 of 32 against it, 26 on the production server and 6 durability tests on the development server. `T-037` is `Done`, so all seven `F-008` Tasks are `Done` and the Owner confirmed the Feature result on `2026-09-06` (`potvrda`). `F-008` is `Done`, `M-001` stands at 12/14 required Features Done, and `F-009` follows in the Owner's order; nothing of it starts before the go-ahead. On `2026-09-06` the Owner asked for the `F-009` Task breakdown without committing or implementing, then had it locked once `F-008` completed: `T-038` through `T-042` are recorded in `Backlog`, operations before screens, weight and body inside the History feature beside the shared chart, and the Feature file carries three readiness questions with recommended answers, chiefly the stale archiving sentence in the weight and body document and what Today shows once the day's weight exists. `T-038` entered git history early through the `T-034` delivery, which the Owner accepted. On `2026-09-06T12:39:15+02:00` the Owner replied `potvrda`: every recommended readiness answer is accepted, so the stale archiving sentence in the weight and body document goes, a measurement type may be renamed, and Today shows the day's recorded weight with a link to Weight once it exists. `F-009` is released and `T-038` delivered its first Task as `94196f3be1f8f7b47b204637a16cc30d0520e916`: the weight reads and the three writes in one function-only migration, and every weekly rule as pure functions in the History domain, where the chart contract moved to `domain/chart.ts` and gained an optional workout, a span, and a companion series so the daily weigh-ins and the weekly averages can be drawn together. The Owner approved it on `2026-09-06`. Its verification then passed unit 174/174 and pgTAP 157/157 before the repository suite refused every call with `42501`: the two `security invoker` helpers were revoked from everyone and never granted to `service_role`, which pgTAP could not catch because it runs as their owner. Replacement `f9edf3a4c3492faf672e12b2dc452d61898a21d7` adds the two grants and inherits the Task approval, and the complete plan then passed: unit 174/174, pgTAP 157/157, repository 8/8, matching types, and a faithful restore. `T-038` is `Done`, so weight has its entries, its Monday-to-Sunday weekly rules, its chart series, and its three writes, and `T-039` then delivered `S19` and `S20` as `37cf5ee592bb6a4851050980c9f6c65a6a73ce0e`: the two summary cards, the four-range selector, the shared chart taught a dashed companion series for the weekly averages, the entry screen with its date default and its `O01` deletion, and `requireLocalDateRouteParam` beside the UUID helper. The Owner approved it on `2026-09-06`; its first verification passed 187 of 188 and failed one prepared assertion that read a collapsed disclosure without opening it, which replacement `d164327f20e2437d2662d8e0b73d38b519b76613` corrects under the inherited approval. The complete plan then passed: unit and component 188/188 and the serialized Chromium and WebKit Weight scenario 2/2 with eight structural captures. `T-039` is `Done`, so Weight is a finished subsection and Body is the last placeholder route; `T-040` then delivered the `MVP-TOD-004` prompt as `4992e617d3d367091332eb178525b2c61e35f0a5`: Today offers the day's weigh-in in a sheet fixed to the local date while the day has none, and shows the recorded value with a link to Weight once it does, reading it beside the Today aggregate so `TodayView` is untouched. The Owner approved it on `2026-09-06`; its first verification failed five Today scenarios for want of the toast provider `MainShell` gives them, which replacement `88abdad827a907fc93c61fd7851cd5d1736057a6` corrects under the inherited approval. The complete plan then passed: unit and component 192/192 and the Chromium and WebKit Today scenario 4/4. `T-040` is `Done`, which finishes weight; `T-041` then delivered the body measurement data as `cde56f00dbc7d98c28cca1c7843dd37000250bab`: one read returning every type with its entries, six writes with named refusals, and the list, detail, and series rules as pure functions in the History domain. The product document loses the stale archiving sentence and gains the rename, both accepted readiness answers. The Owner approved it on `2026-09-06` and the complete plan then passed on the first run: unit 218/218, pgTAP 187/187 including the new body suite 30/30, repository 9/9, matching types, and a faithful restore. `T-041` is `Done`, so only `T-042` remains in `F-009`; `T-042` then delivered `S21` through `S24` as `ae55dd3ef85ce31a692577620736b64a9abf7e54`: the type list, the type form with its read-only unit and its conditional deletion, the detail with three stat cards and the chart, and the entry screen mirroring `S20`. No placeholder route remains in the application. The Owner approved it on `2026-09-06` and the complete plan then passed on the first run: unit and component 235/235 and the Chromium and WebKit Body scenario 2/2 with eight structural captures. All five `F-009` Tasks are `Done`: weight is derived, rendered, and offered on Today, Body has its data and its four screens, and no placeholder route remains in the application. `F-009` is `Done` with the Owner's confirmed result on `2026-09-06`, and `M-001` stands at 13/14 required Features Done. Only `F-010` Local MVP Integration remains. On `2026-09-06` the Owner asked for its Task breakdown: `T-043` through `T-048` are recorded in `Backlog`, the record before the verification and the verification before the close. `T-043` writes the release verification matrix so the remaining scope is observable; `T-044` closes the two items earlier Tasks left to the Owner, the finish review's `Confirmed sets` copy and one production-visibility rule for both test-support routes, recorded as `ADR-0029`; `T-045` and `T-046` deliver the release evidence for `MVP-REL-003`, `MVP-REL-004`, and `MVP-UX-001` through `MVP-UX-003`; `T-048` runs the complete suite once against one approved tree and closes `F-010` and `M-001`. `T-047`, the accepted-reference visual comparison, is `Canceled`: the Owner makes that comparison themselves and `T-048` records what they report. Its analysis is kept in the Feature file, because the accepted package states under `OD-015` that the 404 `v0.3` PNGs were never re-exported and that the `v0.4` corrections change rendered pixels on every screen, 19 of the 202 frames depict archiving and mode-chooser states the project has since removed, and Playwright `1.62.1` bundles exactly the Chromium `151` the references were captured with. The Owner confirmed the breakdown and every remaining recommendation the same day, and then gave the go-ahead (`kreni`). `T-043` delivered the matrix as `d79c08f5bfe3a8e7c796fdd9bb0fe943dea9c601`: 52 of the 57 criteria are covered by an approved delivery's authorized verification, and the 5 that `F-010` owns are the gaps `T-045` and `T-046` close. Reading all 57 against the delivered application surfaced two disagreements, recorded as findings rather than corrected because the criteria document is locked: `MVP-REL-002` still says an active workout uses a focused screen without the bottom navigation, which [ADR-0025](decisions/0025-active-workout-in-the-main-shell.md) superseded and `T-024` delivered, and `MVP-PRG-007` keeps an archiving heading over a body that describes deletion. The Owner approved the delivery and decided both the same day: the bottom navigation must always be visible, and there is no archiving. So the application stands and the documents move, which is `T-049`: it rewrites the `MVP-REL-002` sentence, retitles `MVP-PRG-007`, corrects the architecture document's surviving `focused shells` claim, and amends ADR-0025 to name the criterion it superseded — the omission that let the contradiction live for a day. `T-049` delivered all four as `6cffc618d03198b576374b71db47a6156a2a296d`, and no canonical document outside the frozen design packages and completed Task records still claims a focused shell. The Owner approved it the same day, and `T-044` then delivered the two corrections as `f2a46162b80e747c369e42e4c4e49854ae72cc42`: the finish review says recorded sets, and both test-support routes take one visibility rule under [ADR-0029](decisions/0029-one-visibility-rule-for-test-support-routes.md) — hidden unless the server is started with `PF_ENABLE_TEST_SUPPORT=1`, both routes `force-dynamic`, and the Playwright suite back to one production server with the flag, so the durability spec finally runs against the build the phone user gets. The failure that decision closes was not a wrong rule but an unwritten one. It also reports one further ADR-0027 leftover it did not sweep in: a unit-test name that still says confirmed sets while its case checks a malformed payload. The Owner approved it, and the authorized run then passed 33 of 38: every screen spec on both phones, and all five failures in the durability spec this Task had just moved onto the production server. Its `beforeEach` cleared IndexedDB from the harness page while the harness held the connection open, so `deleteDatabase` blocked and the file deadlocked — a race the development server had been losing in the spec's favour since `T-008`. Replacement `1bee438efe2a7fc4f9e3a399ccaf5ff27a465331` clears from `/today` instead and rejects on `onblocked`, and the complete plan then passed: unit 237/237, components 4/4, and the whole browser suite 38/38 on one production server, with the database matching a fresh seed afterwards. `T-044` is `Done`, so `F-010` stands at 3/6. `T-045` then delivered the two release scenarios as `e59d513da9db55f36655bb3ef9ceb5b3438b90f6`: one dataset touching every persisted category, read back after a reload and again from a freshly opened context that carries no browser storage at all, and a second scenario that renames and deletes definitions, deletes a split, and corrects a historical workout while every saved snapshot stands. Every locator and enum in it was read out of the application before it was written, which caught six wrong assumptions and one context that would have inherited no base URL. The Owner approved it, and the authorized run then needed four test-only replacements before it passed, each a defect in the scenario rather than in the application: a flat set index that relied on an embedded-resource order PostgREST never applies to rows, a split name printed three times on one screen, the `T-037` WebKit hydration race at the one field entry after a navigation, and a teardown that deleted measurement types before their entries and swallowed the refusal `on delete restrict` returns, leaving rows behind on every run. Against `a65a504b64482384c022175230176e983af66ce8` the complete plan passed: the two scenarios 4/4, unit 237/237, components 4/4, the whole browser suite 42/42, and a database identical to its baseline. `MVP-REL-003` and `MVP-REL-004` now carry release evidence, so 54 of the 57 criteria are covered and only the three mobile-interaction criteria remain, which `T-046` closes. Reading the application for the affordances `MVP-UX-002` names, before writing a line of its sweep, `T-046` found finding `R3`: five sentences across four documents require a visible drag handle for reordering, and the application has never had one — every reorderable list offers named 44 by 44 arrow buttons instead — while `MVP-UX-002` also still opened with a current-set sentence that ADR-0027 left behind. The Owner decided both on `2026-09-06`: the arrows are correct and the sentence goes. `T-050` delivered that as `17b12f4233282af479501fc9d0af50052d9ca39a`, and the mobile IA document now carries the reasoning rather than only the rule, which is the absence that produced the finding. The Owner approved it the same day, and `T-046` then delivered its sweep as `300db8bed59d9ce62057064a0dea51ed3ae054e0`: the route inventory read out of `src/app/(main)` so a route added later cannot escape it, all 29 routes measured at 320, 360, 390, and 430 pixels, every numeric field checked for its keyboard, the named reorder controls checked on four lists with their ends unavailable, and six destructive actions each required to open the `O01` confirmation with Cancel leaving the screen where it was. The Owner approved it; the first run failed eight of ten in the seed, which had made the first split proposed and then tried to record it as an alternate, and the database was right to refuse. Against replacement `5c07096fb34801166ddb798345e3fd1b9eb3e117` the sweep passed 10/10 **with no application defect anywhere**, the whole browser suite passed 52/52, and the database matched a fresh seed. **All 57 locked criteria now carry verification against an approved delivery.** Only `T-048` remains, and it delivered its documentation half first as `9d8648d8dfd2acdc24cf60f8731d57821d6d75fb`, because ADR-0021 puts the scoped change before the run: the `T-003-v1` package is annotated as superseded rather than rewritten, with one table naming each accepted decision that has moved past it, and six other statements that had drifted are corrected. The Owner approved it, and **the release run then passed in full on the first attempt**: unit 237/237, components 4/4, pgTAP 187/187, repository 9/9, and the browser suite 52/52 — 489 checks across five suites, all against the same tree, which is what no earlier run could prove because each Feature had only ever been verified against its own delivery. The generated types matched the migrated schema and the database afterwards was identical to a fresh seed. The Owner then reported the visual comparison and confirmed it whole on `2026-09-06` (`vizualno sve izgleda ok`), with no deviation and a stated intent to correct anything they notice later themselves, so it is recorded as their own judgment of the delivered application rather than as a per-frame record the repository did not make. `T-048` is `Done`, and on `2026-09-06` the Owner confirmed both results. **`F-010` and `M-001` are `Done`: the Local MVP is complete** — 14 of 14 Features, all 57 locked criteria verified against approved deliveries, a release run of 489 checks across five suites against one approved tree, and the Owner's own visual comparison. The next work is `M-002`, whose first Feature `F-015` revises three of those criteria to give weight and body their own destination; its breakdown `T-051` through `T-053` is recorded, and the Owner released it on `2026-09-06`. The criteria move first, so nothing is built against a document that contradicts it — the lesson `T-043` and `T-046` both paid for in `F-010`. `T-051` delivered that as `4108680d6e411544190d7c3a8e4cb2752c694c42`: [ADR-0030](decisions/0030-body-is-its-own-destination.md) makes Body the fifth destination, one that reads and corrects but never creates, with entry moved to Today and the centimetre unit reduced to a label. Five criteria are revised and `MVP-TOD-005` is added, so there are 58; the matrix keeps the `M-001` evidence and marks which Task re-verifies each revised criterion. A missed day can no longer be filled in afterwards, which is recorded as the accepted cost rather than left to be discovered. It also found a fourth wording of the focused-shell rule that `T-049` had corrected in three other places, which says something about phrase-based sweeps. `T-052` then built the destination as `c7daf2154aa36097bd0a17034ad3a81fed0c7bff`: the fifth bottom-navigation entry, the History tab bar generalized into `src/shared/ui/subsection-navigation.tsx` and shared by both destinations, six routes moved out of `/history`, the two create routes deleted with the add actions on `S19` and `S23`, the unit reduced to a hint and a label, History down to three tabs, and every spec the move invalidated corrected in the same commit. The Owner approved it, and the authorized run needed three test-only replacements before the whole browser suite passed **52/52**: a string rewrite had missed the four old paths written as escaped regular expressions, and the third seeded row the scenarios now need had shifted a weekly average, a list row, and a chart sentence. Every fault was a scenario still describing the screens as they were before the move, which is what the move was expected to break. `T-052` is `Done`; until `T-053` lands the application records no weigh-in or measurement at all, a deliberate and named gap. `T-053` then delivered the entry path as `c5417723e42e2d04e172fc6f754284fd11ed23df`: a `Body measurements` card beside the weight card, one sheet taking every measurement the day is missing, and a new `create_measurement_entries` migration so the values are written in one transaction, which is what `MVP-TOD-005` means by recording them together. The Task also discloses a test-gate breach: the unit suite was run during `T-052` and `T-053` to find which tests the route move had broken, which the governance forbids before approval. The Owner approved the delivery and **the complete plan then passed on the first run**: unit 241/241, pgTAP 191/191 including the four assertions that the day's measurements are written together or not at all, repository 9/9, and the whole browser suite 52/52, with the database identical to its baseline. All three `F-015` Tasks are `Done`. The Owner confirmed the result as delivered on `2026-09-06`, which also decides the one open item: the Today sheet keeps requiring every missing measurement, so a partial day stays unrecordable. **`F-015` and `M-002` are `Done`.** Weight and body have a destination of their own, entry lives on Today, and the six criteria the change touched were revised by explicit decision ahead of the Tasks that built against them. The staleness reached every spec written before `T-018`, five files and not two, so all were brought to the current screens with their coverage mapped onto the accepted replacements for archiving, set confirmation, the restored banner, and the focused shell. The delivery provides: two identity-grouped reads in the database, and every product rule as pure functions in the History domain. Preparing its browser scenario surfaced `T-037`: two older browser specs still call the archiving artifacts `T-021` removed, so the browser suite cannot run as a whole. The accepted answers add a never-nulled identity snapshot for exercises and splits, keep recorded active duration out of historical editing, allow only the incomplete-to-completed direction, run corrections as ordinary transactional server actions, and give `F-008` the History shell with placeholder Weight and Body routes for `F-009`. The reconfirmed working order is `F-013`, then `F-014` with `T-028` before `T-029`, then `F-012`, then `F-008`. The Owner confirmed on `2026-09-05` that both mechanisms are built, with the seed as the fallback when no snapshot exists. `F-014` then delivers the Owner's two new model corrections: two exercise types with assistance under bodyweight, and a set recorded by its entered values with no confirmation control. The Owner set that order on 2026-09-05 and reconfirmed it the same day, when the `Next` numbering was corrected to match the Feature files.

## Implementation status

`F-004`, `F-005`, and `F-006`, including `T-005` through `T-013`, are `Done`. Exact T-014 delivery `4f924d51af2e55681f2e5a517a8963bc58048d81` and exact T-017 correction `8d5779258505bb94383368e13eff97a4346320ca` passed their complete verification. `F-007` is complete with the Owner's confirmed result: exact T-015 second replacement `f52c0447db67007fba7cce8d0c790164e8447fe9` and exact T-016 second replacement `441a87046409d2970de72e5c3f9c1448c4423a4d` both passed their complete verification, and `M-001` stands at `7/11 required Features Done` after `F-011` was added. All nine corrections are complete, so the delivered behavior and the accepted specification agree again. Deployment configuration does not exist yet.
